/**
 * Разрешение RBAC, которым закрыты чужие профили, и его действия.
 *
 * Одно место на все сценарии: политика полей ссылается на действия чтения,
 * запись профиля — на update, удаление — на delete. Раньше эти строки
 * лежали по разным dto, и удаление импортировало константу из файла про
 * просмотр профиля — связь, которой быть не должно.
 *
 * Названия обязаны совпадать со строками в таблице permissions: связь
 * между кодом и конфигурацией держится на них.
 */
export const USERS_PERMISSION = 'users';

export const USERS_ACTIONS = {
  /** Базовое право на чужой профиль */
  Read: 'read',
  /** Плюс почта */
  ReadEmail: 'read_email',
  /** Плюс служебные отметки */
  ReadActivity: 'read_activity',
  /** Правка чужого профиля */
  Update: 'update',
  /** Удаление чужого аккаунта */
  Delete: 'delete',
  /** Список пользователей для администратора */
  List: 'list',
} as const;
